
group 'com.epam.esm'
version '1.0'

dependencies {
    compile project(":persistence")
}

apply plugin: 'jacoco'

jacocoTestReport {
    dependsOn test
}

jacocoTestReport {
    reports {
        html.destination file("${buildDir}/jacocoHtml")
    }
}
